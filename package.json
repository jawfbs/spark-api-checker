{
  "name": "spark-api-checker",
  "version": "1.0.0",
  "description": "Vercel template that tests connectivity to the Spark API by FBS (replication endpoint).",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^16.2.4",
"react": "^19.0.0",
"react-dom": "^19.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
